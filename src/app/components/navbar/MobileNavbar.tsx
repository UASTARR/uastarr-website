'use client';
import { useState } from 'react';

const MobileNavbar = () => {
    const [isOpen, setIsOpen] = useState(false);
    return (
        <>
        </>
    );
}

export default MobileNavbar;